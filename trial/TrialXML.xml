<?xml version="1.0" encoding="UTF-8" ?>
<Module>
  <ModulePrefs title="Talent Sprint">
    <Require feature="rpc"/>
    <Require feature="views"/>
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
      <script src="//talkgadget.google.com/hangouts/_/api/hangout.js?v=1.1"></script>
  
  <img src="https://github.com/tanujp/GadgetXML/raw/master/talent_sprint.gif"/>	 
  <h3>Welcome to TalentSprint</h3>	
  <p id="participantsDiv">No Trainees joined</p> 
  <p id="Trainer">No Id</p>
  <p id="No"></p>
<script>	
var NoOfParticipants=0;
function showParticipants() {
  ++NoOfParticipants;
  var participants = gapi.hangout.getParticipants();
  var retVal = '<h4>Trainees: </h4><ul>';
  var participantIds = gapi.hangout.participants.getParticipantId();
  for(var i=0;i<participants.length;i++){
    ++NoOfParticipants;
    var participantId=participantsId[i];
    var participant = participants[i];

    if (!participant.person) {
      retVal += '<li>A participant not running this app</li>';
    }
    retVal += '<li>' + participant.person.displayName + '</li>';
  }

  retVal += '</ul>';

  var div = document.getElementById('participantsDiv');

  div.innerHTML = retVal;
  
  document.getElemenetById("Trainer").innerHTML=participantId;
  document.getElementById("No").innerHTML=NoOfParticipants;
  //showVideoFeed();
  
}

//function showVideoFeed(){

//switch(NoOfParticipants)
  //{
   // Case 1 : 
  //}

//}

 // When Gadget is ready...
  function init() {
  console.log('init');
  
  //When API is Ready
  gapi.hangout.onApiReady.add(
      function(eventObj) {
        if (eventObj.isApiReady) {
            console.log('API is Ready');
            showParticipants();
           
  //When People leave a Hangout
  gapi.hangout.onParticipantsChanged.add(
      function(eventObj){
      console.log('Participants Changed');
      showParticipants();
      });
      
      }  
      });
  }

  // Wait for gadget to load.                                                       
  gadgets.util.registerOnLoadHandler(init);

</script>   	
    ]]>
  </Content>
</Module>